    <h1>Blog Articles</h1>
    <ul>
        {{#each blogs}}
            <li>
                <h2>{{this.title}}</h2>
                <p>{{this.content}}</p>
                <a href="/blogPost/{{this.slug}}">Read More</a>
            </li>
        {{/each}}
    </ul> 